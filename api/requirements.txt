# requirements.txt — Dependências do serviço API (Python/FastAPI)
#
# Instale via: pip install -r requirements.txt
# Congele as versões exatas com: pip freeze > requirements.lock.txt

# Web framework
fastapi==0.115.6

# Servidor ASGI (Uvicorn)
uvicorn[standard]==0.34.0

# Banco de dados (opcional para esta fase)
# SQLAlchemy para ORM
sqlalchemy==2.0.36

# PostgreSQL driver
psycopg2-binary==2.9.10

# Migrações
alembic==1.14.0

# Pydantic para validação de dados
pydantic==2.10.4
pydantic-settings==2.7.0

# Validação de email (usada pelo pydantic EmailStr)
email-validator==2.2.0

# Autenticação (JWT)
PyJWT==2.10.1

# Hashing de senhas (passlib + bcrypt)
# passlib é o wrapper; bcrypt é o algoritmo em si.
# IMPORTANTE: passlib 1.7.4 quebra com bcrypt >= 4.1 (erro no self-test com
# senha > 72 bytes). Por isso fixamos bcrypt em 4.0.1.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Leitura de variáveis de ambiente
python-dotenv==1.0.1

# CORS (permitir que o frontend acesse a API)
python-multipart==0.0.20

# Envio de e-mail (recuperação de senha) — SDK oficial do Resend
# https://resend.com/docs/send-with-python
resend==2.34.0
